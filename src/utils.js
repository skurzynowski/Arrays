export const filterById = (id, posts) => {
  return posts;
};

export const findByTitle = (title, posts) => {
  return posts;
};

export const addImg = posts => {
  return posts;
};

export const getNumberOfPosts = posts => {
  return 1000000;
};

export const everyHasImg = posts => {
  return false;
};

export const anyHasImg = posts => {
  return false;
};

//img link has id of img inside replace this with random number 0 - 100 range
export const replaceFirsNumber = string => {
  return string;
};
